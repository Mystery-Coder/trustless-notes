% Encoding and Fonts
\usepackage[utf8]{inputenc}
\usepackage{times}

% Page Layout
\usepackage[a4paper]{geometry}
\geometry{left=0.75in, right=0.75in, top=1in, bottom=1in}


% Text Justification and Spacing Quality
\usepackage{ragged2e}    % Ensures proper full justification
\usepackage{microtype}   % Improves spacing and justification

% Spacing
\usepackage{setspace}
\onehalfspacing

% Graphics
\usepackage{graphicx}
\graphicspath{{figures/}}

% Tables
\usepackage{array}
\usepackage{tabularx}
\usepackage{booktabs}

% Chapter and Section Formatting
\usepackage{titlesec}
\titleformat{\chapter}[display]
    {\normalfont\large\bfseries\centering}
    {\chaptertitlename\ \thechapter}
    {10pt}
    {\large}
\titlespacing*{\chapter}{0pt}{-20pt}{20pt}

% Header and Footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,
    urlcolor=blue,
    citecolor=black
}

% Lists
\usepackage{enumitem}

% Code Listings (if needed)
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single
}

% Caption formatting
\usepackage{caption}
\captionsetup{font=small, labelfont=bf}

% Grids
\usepackage{graphicx}
\usepackage{subcaption}